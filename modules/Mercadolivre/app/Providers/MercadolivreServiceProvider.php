<?php

namespace Mercadolivre\Providers;

use Illuminate\Support\ServiceProvider;

class MercadolivreServiceProvider extends ServiceProvider
{
    /**
     * Indicates if loading of the provider is deferred.
     *
     * @var bool
     */
    protected $defer = false;

    /**
     * Boot the application events.
     *
     * @return void
     */
    public function boot()
    {
        $this->registerTranslations();
        $this->registerConfig();
        $this->registerViews();
    }

    /**
     * Register the service provider.
     *
     * @return void
     */
    public function register()
    {
        //
    }

    /**
     * Register config.
     *
     * @return void
     */
    protected function registerConfig()
    {
        $this->publishes([
            __DIR__.'/../../config/config.php' => config_path('mercadolivre.php'),
        ]);
        $this->mergeConfigFrom(
            __DIR__.'/../../config/config.php', 'mercadolivre'
        );
    }

    /**
     * Register views.
     *
     * @return void
     */
    public function registerViews()
    {
        $viewPath = base_path('resources/views/modules/mercadolivre');

        $sourcePath = __DIR__.'/../';

        $this->publishes([
            $sourcePath => $viewPath
        ]);

        $this->loadViewsFrom(array_merge(array_map(function ($path) {
            return $path . '/modules/mercadolivre';
        }, \Config::get('view.paths')), [$sourcePath]), 'mercadolivre');
    }

    /**
     * Register translations.
     *
     * @return void
     */
    public function registerTranslations()
    {
        $langPath = base_path('resources/lang/modules/mercadolivre');

        if (is_dir($langPath)) {
            $this->loadTranslationsFrom($langPath, 'mercadolivre');
        } else {
            $this->loadTranslationsFrom(__DIR__ .'/../', 'mercadolivre');
        }
    }

    /**
     * Get the services provided by the provider.
     *
     * @return array
     */
    public function provides()
    {
        return [];
    }
}
